{
  "stt": {
    "module": "ovos-stt-plugin-server",
    "//": "With no 'urls' set, the proxy plugin uses the public OVOS community Whisper servers (stt.openvoiceos.pt/stt, stt.smartgic.io/fasterwhisper/stt) with automatic failover. Set 'urls' to use a private server.",
    "ovos-stt-plugin-server": {
      "verify_ssl": true
    }
  }
}
